const authenticationRoutes = require('../routes/authentication-routes');
const accountController = require('../routes/account-routes');

module.exports = {
    authenticationRoutes,
    accountController
}